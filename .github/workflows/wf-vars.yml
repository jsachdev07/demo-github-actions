name: Testing Workflow Variables

on:
  workflow_dispatch

env:
  cloud: google-cloud

jobs:
  greeting_job:
    runs-on: ubuntu-latest
    env:
      Greeting: Hello
    steps:
      - name: "Say Hello All it's Monday"
        run: echo "$Greeting $First_Name I'm using $cloud!"
        env:
          First_Name: All
